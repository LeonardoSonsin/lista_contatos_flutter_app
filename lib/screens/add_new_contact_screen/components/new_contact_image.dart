import 'package:flutter/material.dart';

class NewContactImage extends StatelessWidget {
  const NewContactImage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Icon(
      Icons.person_pin,
      size: 100,
    );
  }
}
